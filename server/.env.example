# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration
# These settings are for the local tunnel endpoint
POSTGRES_USER=user
POSTGRES_PASSWORD=
POSTGRES_DB=db
POSTGRES_HOST=localhost  # Local tunnel endpoint
POSTGRES_PORT=5432      # Local tunnel port

# PostgreSQL SSL Tunnel Configuration
POSTGRES_REMOTE_HOST=your-remote-db-host  # The actual remote database host
POSTGRES_REMOTE_PORT=5432                 # The actual remote database port
SSH_HOST=your-bastion-host               # SSH tunnel host
SSH_USER=your-ssh-user                   # SSH tunnel user
SSH_PORT=22                              # SSH tunnel port
# SSH_KEY_PATH=/path/to/your/private/key # Uncomment and set if using key-based auth
# SSH_PASSWORD=                          # Uncomment and set if using password auth

# Auth0 Configuration
# AUTH0_ISSUER_URL should be https://{your-auth0-domain}/
# Example: if VITE_AUTH0_DOMAIN=your-domain.auth0.com, then AUTH0_ISSUER_URL=https://your-domain.auth0.com/
AUTH0_ISSUER_URL=https://${VITE_AUTH0_DOMAIN}/

# AUTH0_AUDIENCE should match your Auth0 API identifier (typically your client ID)
# Use the same value as AUTH0_CLIENT_ID from the main .env file
AUTH0_AUDIENCE=${AUTH0_CLIENT_ID}

# Security
CORS_ORIGIN=http://localhost:5173 
